## What does this change?

<!-- One or two sentences. -->

## Checklist

- [ ] Tested against a real Actual Budget instance
- [ ] `pnpm format && pnpm check && pnpm lint` pass locally
- [ ] Toggling the setting off leaves no listeners/DOM nodes/observers behind
- [ ] `label`/`description`/`icon`/`group` are set, and are plain string literals (not computed — see [CONTRIBUTING.md](../CONTRIBUTING.md))
- [ ] Screenshot or short clip attached, for anything visually observable

<!--
PR title in conventional-commit form (feat:/fix:/chore:) — this repo's
release automation builds the changelog from commit/PR titles.
-->
